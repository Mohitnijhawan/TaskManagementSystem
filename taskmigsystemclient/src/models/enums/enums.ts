export enum UserRole{
    Admin = 1,
    User = 2
}

  export enum StatusTask
  {
      Pending=1,
      InProgress=2,
      Completed=3
  }

export enum TaskPriority
 {
     Low=1,
     Medium=2,
     High=3
 }